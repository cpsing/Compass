Step 1 — 启动 Claude Code 并喂入系统提示

  cd /Users/xingbaichao/Documents/projects-prod/compass-test
  claude

  进入交互后，第一条消息粘下面这段（系统提示，让 AI 主动用 Compass 工具）：

  You have access to Compass MCP tools (compass_*) that persist this project's
  state across AI sessions. Use them aggressively:

  1. At the start of any task, call compass_list_features first to see what
     already exists.
  2. Before non-trivial code changes, call compass_start_ai_run with intent +
     short plan.
  3. After finishing, call compass_finish_ai_run with summary + files_touched.
  4. Use compass_add_code_todo for any deferred sub-tasks.
  5. You can only create kind='task' nodes; modules and features are
     created by the user in the dashboard.
  6. You cannot set status='verified' — that's a user-only boundary.

  Confirm you understand by calling compass_list_features now.

  期望反应：AI 应该立即调一次 compass_list_features，返回空（项目刚建）。如果没调，说明系统提示力度不够——加一句"Before responding, call
  compass_list_features."

  Step 2 — 让 AI 做一个真实的小活

  在 Dashboard 先建个 feature 给它做（AI 只能建 task，不能建 module/feature）：

  # 另开一个终端
  ▎ cd /Users/xingbaichao/Documents/projects-prod/Compass
  ▎ npm run dev:web
  ▎ 浏览器 http://localhost:3737 → 应该看到刚刚 AI 调 compass_list_features 时自动注册的 compass-test 项目。

  进入项目页 → 点 "+ Add module"（比如建 Sandbox）→ 点 "+ Add feature under Sandbox"（比如建 Hello endpoint）。

  回到 claude 终端，发：

  Please implement a simple POST /hello endpoint in this project that returns
  {"message": "hi"}. It corresponds to the "Hello endpoint" feature we just
  created. Make a minimal Express app for it.

  期望反应（按系统提示）：
  1. AI 先调 compass_list_features 找到 "Hello endpoint" 节点 id
  2. 调 compass_start_ai_run({feature_node_id, intent:'implement', user_prompt_summary:'...', plan:'...'})
  3. 真的写代码（创建几个文件）
  4. 调 compass_finish_ai_run({run_status:'completed', next_status:'ai_completed', summary, files_touched})

  Step 3 — 验证

  COMPASS_PROJECT_ROOT=/Users/xingbaichao/Documents/projects-prod/compass-test \
    /Users/xingbaichao/Documents/projects-prod/Compass/bin/compass-cli status

  应该看到：
  - activity_events 里 mcp_call ≥ 3（list_features + start + finish）
  - recent ai_runs 第一行是 running 或 completed · origin=mcp · client_type=claude_code
  - attention needed 里出现 ai_completed 的 Hello endpoint

  或者打开 Dashboard 的 Hello endpoint 详情页，应该看到 AIRun 时间线 + plan/summary + files_touched。

  如果 AI 没调工具怎么办

  按这个顺序排查：

  1. 看 compass-cli status 有没有任何 mcp_call 事件——如果完全没有，说明 Claude Code 根本没让 AI 看到工具
  2. 在 claude 里直接问："What tools do you have available?" → 它应该列出 13 个 compass_* 工具
  3. 若工具可见但 AI 不主动调，加强系统提示——比如改成 "You MUST call compass_list_features before any other action."

  先做 Step 1，把第一条对话粘过来给我看 AI 是否调了 compass_list_features。
